<table class="w3-table w3-striped w3-bordered">
	<tr>
	  <th>Name</th>
	  <th>More..</th>
	</tr>
	<% @categories.each do |t| %>
		<tr>
		  <td><%= t.name %></td>
		  <td><%= link_to "Explore", category_path(t) %></td>
		</tr>
	<% end %>
</table>
